{
  "name": "date-talk-simple",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "private": true,
  "type": "module",
  "scripts": {
    "build:parser": "peggy --format es --output src/parser.js src/grammar.pegjs",
    "build": "npm run build:parser && tsc -p tsconfig.json && cp src/parser.js dist/parser.js",
    "test": "npm run build:parser && tsc -p tsconfig.test.json && cp src/parser.js dist/parser.js && node --test dist/tests/*.test.js",
    "dev": "node --enable-source-maps dist/cli.js"
  },
  "dependencies": {
    "luxon": "^3.5.0"
  },
  "devDependencies": {
    "@types/luxon": "^3.7.1",
    "@types/node": "^22.19.17",
    "peggy": "^4.2.0",
    "typescript": "^5.6.3"
  }
}
